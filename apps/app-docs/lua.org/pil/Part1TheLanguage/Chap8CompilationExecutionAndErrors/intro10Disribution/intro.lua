-- If it complains about bad arguments, then you have dynamic linking facility. 
-- Otherwise, the error message indicates that this facility is not supported or not installed.)
print(package.loadlib())
