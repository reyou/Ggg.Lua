tab = {
    math.sin(1),
    math.sin(2),
    math.sin(3),
    math.sin(4),
    math.sin(5),
    math.sin(6),
    math.sin(7),
    math.sin(8)
}
for i, val in ipairs(tab) do
    print("sin() of " .. i .. " " .. val)
end
